严重bug：
1. 目前图片的关键点变动和新增框操作均不会被保存到txt，新增框也不会显示在标注列表，请修复这个问题
2. bbox目前只能增删无法调整，应该设置四个角为可调整状态，可以通过鼠标拖动四角调整框
3. 项目打开页面，选中历史项目无效，无法打开，必须重新设置项目，请修复这个问题

未完成的特性：
1. 打开一张新图片时，如果图片有标注，则txt中排第一的框应该被默认选中，而不是要手动选
2. 复制关键点的方案还未实现好，原有的复制关键点的方案我再详细描述下：处于关键点标注状态时，鼠标拖动可以框选关键点，框选完关键点点击框外区域或者按ctrl+c会对框选的关键点进行复制，复制实际保留的是关键点基础信息(位置，kpN_vis，关键点id)，重新框选会覆盖上次复制的内容；当处于关键点标注状态时使用快捷键ctrl+v，会覆盖掉当前对应点的标注，此操作可以跨图进行
3. 多类目标检测没有支持到，yaml提供多names(即多类目标)时，新增框时弹出类别的选择（这个弹出窗口会在选择完类别后自动关闭），已创建的框也可以在标注列表双击弹出选择
4. 标注列表一栏：在选中框时展开关键点信息，双击对应关键点弹出kpN_vis选择窗口，选择完毕后关闭选择窗口

体验优化方案：
1. 按箭头下键时如果在bbox标注状态，则自动退出bbox标注；按上键时如果在关键点标注状态，则切换为bbox标注状态；未选中框时想进入关键点标注目前会弹出错误提示，将这个提示放到左下角的常规提示区，不再作为单独窗口弹出
2. 把图像中绘制的各个关键点的名称改为对应的快捷键名称
3. 添加ctrl+z撤回和ctrl+shift+z复原的功能，撤回最多支持50次操作，切换图片时失效
